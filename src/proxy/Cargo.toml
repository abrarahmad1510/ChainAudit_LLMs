[package]
name = "verillm-proxy"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
proxy-wasm = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
blake3 = "1.5"
log = "0.4"
hex = "0.4"

[profile.release]
lto = true
opt-level = 3
